# convertX
This is Djnago Based website that handles different document conversions.


After Clone

pip install django
pip install pdf2docx
pip install docx2pdf
pip install fpdf
pip install PyPDF2
pip install xlwings
create folder name 'uploads' if not exists